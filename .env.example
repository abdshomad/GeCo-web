# GeCo Web App Configuration
# Copy this file to .env and modify as needed

# Host to bind the web app (default: 0.0.0.0)
HOST=0.0.0.0

# Port to run the web app (default: 8013)
PORT=8013

# Optional: Model path (default: GeCo.pth)
# MODEL_PATH=GeCo.pth
